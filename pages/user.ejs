<%- include('includes/head') %>
<main class="w-full h-screen bg-gray-100 flex flex-col justify-center items-center">
        <div class="h-64 w-80 rounded-xl font-mono bg-yellow-200 flex flex-col text-center justify-center items-center shadow-xl shadow-yellow-800 ease-in duration-300 hover:shadow-none">
            <h1 class="w-full bg-yellow-300 mb-4 text-xl font-bold"><%= userProfile.username %></h1>
            <img class="w-18 h-16 rounded-xl" src="<%= userProfile.avatar %>" alt="">
            <h2 class="mt-4"><%= userProfile.email %></h2>
            <h3 class="opacity-50 mt-4">password: <%= userProfile.password %></h3>
        </div>
    <a href="/"><button class="w-80 mt-8 bg-orange-300 flex justify-center items-center border border-black-50 rounded-lg shadow-lg shadow-orange-800 ease-in duration-300 hover:bg-orange-400 hover:shadow-none">Буцах</button></a>
    </main>
<%- include('includes/foot') %>